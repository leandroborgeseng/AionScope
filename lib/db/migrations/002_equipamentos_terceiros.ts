/**
 * Tabela equipamentos_terceiros (CREATE IF NOT EXISTS em lib/db/schema.ts).
 *
 * Histórico: acompanhamento local de equipamentos TERCEIRO (médico/responsável).
 * Deixou de ser usada — a tela /cadastros/terceiros lê só a API GlobalThings.
 * A tabela pode permanecer órfã no SQLite; drop opcional em migration futura.
 */
export {};
